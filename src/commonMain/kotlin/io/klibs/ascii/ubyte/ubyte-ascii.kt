package io.klibs.ascii.ubyte

/**
 * ASCII Null
 *
 * ```
 * '\u0000'
 * ```
 *
 * @see ASCII_NIL
 * @see ASCII_NULL
 */
const val ASCII_NUL: UByte = 0u

/**
 * ASCII Null
 *
 * ```
 * '\u0000'
 * ```
 *
 * @see ASCII_NUL
 * @see ASCII_NULL
 *
 * @since 1.1.0
 */
const val ASCII_NIL: UByte = 0u

/**
 * ASCII Null
 *
 * ```
 * '\u0000'
 * ```
 *
 * @see ASCII_NUL
 * @see ASCII_NIL
 *
 * @since 1.1.0
 */
const val ASCII_NULL: UByte = 0u

/**
 * ASCII Start of Heading
 *
 * ```
 * '\u0001'
 * ```
 *
 * @see ASCII_START_OF_HEADING
 */
const val ASCII_SOH: UByte = 1u

/**
 * ASCII Start of Heading
 *
 * ```
 * '\u0001'
 * ```
 *
 * @see ASCII_SOH
 *
 * @since 1.1.0
 */
const val ASCII_START_OF_HEADING: UByte = 1u

/**
 * ASCII Start of Text
 *
 * ```
 * '\u0002'
 * ```
 *
 * @see ASCII_START_OF_TEXT
 */
const val ASCII_STX: UByte = 2u

/**
 * ASCII Start of Text
 *
 * ```
 * '\u0002'
 * ```
 *
 * @see ASCII_STX
 *
 * @since 1.1.0
 */
const val ASCII_START_OF_TEXT: UByte = 2u

/**
 * ASCII End of Text
 *
 * ```
 * '\u0003'
 * ```
 *
 * @see ASCII_END_OF_TEXT
 */
const val ASCII_ETX: UByte = 3u

/**
 * ASCII End of Text
 *
 * ```
 * '\u0003'
 * ```
 *
 * @see ASCII_ETX
 *
 * @since 1.1.0
 */
const val ASCII_END_OF_TEXT: UByte = 3u

/**
 * ASCII End of Transmission
 *
 * ```
 * '\u0004'
 * ```
 *
 * @see ASCII_END_OF_TRANSMISSION
 *
 * @since 1.1.0
 */
const val ASCII_EOT: UByte = 4u

/**
 * ASCII End of Transmission
 *
 * ```
 * '\u0004'
 * ```
 *
 * @see ASCII_EOT
 */
const val ASCII_END_OF_TRANSMISSION: UByte = 4u

/**
 * ASCII Enquiry
 *
 * ```
 * '\u0005'
 * ```
 *
 * @see ASCII_ENQUIRY
 */
const val ASCII_ENQ: UByte = 5u

/**
 * ASCII Enquiry
 *
 * ```
 * '\u0005'
 * ```
 *
 * @see ASCII_ENQ
 *
 * @since 1.1.0
 */
const val ASCII_ENQUIRY: UByte = 5u

/**
 * ASCII Acknowledge
 *
 * ```
 * '\u0006'
 * ```
 *
 * @see ASCII_ACKNOWLEDGE
 */
const val ASCII_ACK: UByte = 6u

/**
 * ASCII Acknowledge
 *
 * ```
 * '\u0006'
 * ```
 *
 * @see ASCII_ACK
 *
 * @since 1.1.0
 */
const val ASCII_ACKNOWLEDGE: UByte = 6u

/**
 * ASCII Bell Character
 *
 * ```
 * '\u0007'
 * ```
 */
const val ASCII_BELL: UByte = 7u

/**
 * ASCII Backspace
 *
 * ```
 * '\u0008'
 * ```
 *
 * @see ASCII_BACKSPACE
 */
const val ASCII_BS: UByte = 8u

/**
 * ASCII Backspace
 *
 * ```
 * '\u0008'
 * ```
 *
 * @see ASCII_BS
 *
 * @since 1.1.0
 */
const val ASCII_BACKSPACE: UByte = 8u

/**
 * ASCII Horizontal Tab
 *
 * ```
 * '\t'
 * ```
 */
const val ASCII_TAB: UByte = 9u

/**
 * ASCII Line Feed
 *
 * ```
 * '\n'
 * ```
 *
 * @see ASCII_LINE_FEED
 */
const val ASCII_LF: UByte = 10u

/**
 * ASCII Line Feed
 *
 * ```
 * '\n'
 * ```
 *
 * @see ASCII_LF
 *
 * @since 1.1.0
 */
const val ASCII_LINE_FEED: UByte = 10u

/**
 * ASCII Vertical Tab
 *
 * ```
 * '\u000B'
 * ```
 *
 * @see ASCII_VERTICAL_TAB
 */
const val ASCII_VT: UByte = 11u

/**
 * ASCII Vertical Tab
 *
 * ```
 * '\u000B'
 * ```
 *
 * @see ASCII_VT
 *
 * @since 1.1.0
 */
const val ASCII_VERTICAL_TAB: UByte = 11u

/**
 * ASCII Form Feed
 *
 * ```
 * '\u000C'
 * ```
 *
 * @see ASCII_FORM_FEED
 */
const val ASCII_FF: UByte = 12u

/**
 * ASCII Form Feed
 *
 * ```
 * '\u000C'
 * ```
 *
 * @see ASCII_FF
 *
 * @since 1.1.0
 */
const val ASCII_FORM_FEED: UByte = 12u

/**
 * ASCII Carriage Return
 *
 * ```
 * '\r'
 * ```
 *
 * @see ASCII_CARRIAGE_RETURN
 */
const val ASCII_CR: UByte = 13u

/**
 * ASCII Carriage Return
 *
 * ```
 * '\r'
 * ```
 *
 * @see ASCII_CR
 *
 * @since 1.1.0
 */
const val ASCII_CARRIAGE_RETURN: UByte = 13u

/**
 * ASCII Shift Out
 *
 * ```
 * '\u000E'
 * ```
 *
 * @see ASCII_SHIFT_OUT
 */
const val ASCII_SO: UByte = 14u

/**
 * ASCII Shift Out
 *
 * ```
 * '\u000E'
 * ```
 *
 * @see ASCII_SO
 *
 * @since 1.1.0
 */
const val ASCII_SHIFT_OUT: UByte = 14u

/**
 * ASCII Shift In
 *
 * ```
 * '\u000F'
 * ```
 *
 * @see ASCII_SHIFT_IN
 */
const val ASCII_SI: UByte = 15u

/**
 * ASCII Shift In
 *
 * ```
 * '\u000F'
 * ```
 *
 * @see ASCII_SI
 *
 * @since 1.1.0
 */
const val ASCII_SHIFT_IN: UByte = 15u

/**
 * ASCII Data Link Escape
 *
 * ```
 * '\u0010'
 * ```
 *
 * @see ASCII_DATA_LINK_ESCAPE
 */
const val ASCII_DLE: UByte = 16u

/**
 * ASCII Data Link Escape
 *
 * ```
 * '\u0010'
 * ```
 *
 * @see ASCII_DLE
 *
 * @since 1.1.0
 */
const val ASCII_DATA_LINK_ESCAPE: UByte = 16u

/**
 * ASCII Device Control 1
 *
 * ```
 * '\u0011'
 * ```
 *
 * @see ASCII_DEVICE_CONTROL_1
 */
const val ASCII_DC1: UByte = 17u

/**
 * ASCII Device Control 1
 *
 * ```
 * '\u0011'
 * ```
 *
 * @see ASCII_DC1
 *
 * @since 1.1.0
 */
const val ASCII_DEVICE_CONTROL_1: UByte = 17u

/**
 * ASCII Device Control 2
 *
 * ```
 * '\u0012'
 * ```
 *
 * @see ASCII_DEVICE_CONTROL_2
 */
const val ASCII_DC2: UByte = 18u

/**
 * ASCII Device Control 2
 *
 * ```
 * '\u0012'
 * ```
 *
 * @see ASCII_DC2
 *
 * @since 1.1.0
 */
const val ASCII_DEVICE_CONTROL_2: UByte = 18u

/**
 * ASCII Device Control 3
 *
 * ```
 * '\u0013'
 * ```
 *
 * @see ASCII_DEVICE_CONTROL_3
 */
const val ASCII_DC3: UByte = 19u

/**
 * ASCII Device Control 3
 *
 * ```
 * '\u0013'
 * ```
 *
 * @see ASCII_DC3
 *
 * @since 1.1.0
 */
const val ASCII_DEVICE_CONTROL_3: UByte = 19u

/**
 * ASCII Device Control 4
 *
 * ```
 * '\u0014'
 * ```
 *
 * @see ASCII_DEVICE_CONTROL_4
 */
const val ASCII_DC4: UByte = 20u

/**
 * ASCII Device Control 4
 *
 * ```
 * '\u0014'
 * ```
 *
 * @see ASCII_DC4
 *
 * @since 1.1.0
 */
const val ASCII_DEVICE_CONTROL_4: UByte = 20u

/**
 * ASCII Negative Acknowledge
 *
 * ```
 * '\u0015'
 * ```
 *
 * @see ASCII_NEGATIVE_ACKNOWLEDGE
 */
const val ASCII_NAK: UByte = 21u

/**
 * ASCII Negative Acknowledge
 *
 * ```
 * '\u0015'
 * ```
 *
 * @see ASCII_NAK
 *
 * @since 1.1.0
 */
const val ASCII_NEGATIVE_ACKNOWLEDGE: UByte = 21u

/**
 * ASCII Synchronize
 *
 * ```
 * '\u0016'
 * ```
 *
 * @see ASCII_SYNCHRONOUS_IDLE
 */
const val ASCII_SYN: UByte = 22u

/**
 * ASCII Synchronize
 *
 * ```
 * '\u0016'
 * ```
 *
 * @see ASCII_SYN
 *
 * @since 1.1.0
 */
const val ASCII_SYNCHRONOUS_IDLE: UByte = 22u

/**
 * ASCII End of Transmission Block
 *
 * ```
 * '\u0017'
 * ```
 *
 * @see ASCII_END_OF_TRANSMISSION_BLOCK
 */
const val ASCII_ETB: UByte = 23u

/**
 * ASCII End of Transmission Block
 *
 * ```
 * '\u0017'
 * ```
 *
 * @see ASCII_ETB
 *
 * @since 1.1.0
 */
const val ASCII_END_OF_TRANSMISSION_BLOCK: UByte = 23u

/**
 * ASCII Cancel
 *
 * ```
 * '\u0018'
 * ```
 */
const val ASCII_CANCEL: UByte = 24u

/**
 * ASCII End of Medium
 *
 * ```
 * '\u0019'
 * ```
 *
 * @see ASCII_END_OF_MEDIUM
 */
const val ASCII_EM: UByte = 25u

/**
 * ASCII End of Medium
 *
 * ```
 * '\u0019'
 * ```
 *
 * @see ASCII_EM
 *
 * @since 1.1.0
 */
const val ASCII_END_OF_MEDIUM: UByte = 25u

/**
 * ASCII Substitute
 *
 * ```
 * '\u001A'
 * ```
 *
 * @see ASCII_SUBSTITUTE
 */
const val ASCII_SUB: UByte = 26u

/**
 * ASCII Substitute
 *
 * ```
 * '\u001A'
 * ```
 *
 * @see ASCII_SUB
 *
 * @since 1.1.0
 */
const val ASCII_SUBSTITUTE: UByte = 26u

/**
 * ASCII Escape
 *
 * ```
 * '\u001B'
 * ```
 *
 * @see ASCII_ESCAPE
 */
const val ASCII_ESC: UByte = 27u

/**
 * ASCII Escape
 *
 * ```
 * '\u001B'
 * ```
 *
 * @see ASCII_ESC
 *
 * @since 1.1.0
 */
const val ASCII_ESCAPE: UByte = 27u

/**
 * ASCII File Separator
 *
 * ```
 * '\u001C'
 * ```
 *
 * @see ASCII_FILE_SEPARATOR
 */
const val ASCII_FS: UByte = 28u

/**
 * ASCII File Separator
 *
 * ```
 * '\u001C'
 * ```
 *
 * @see ASCII_FS
 *
 * @since 1.1.0
 */
const val ASCII_FILE_SEPARATOR: UByte = 28u

/**
 * ASCII Group Separator
 *
 * ```
 * '\u001D'
 * ```
 *
 * @see ASCII_GROUP_SEPARATOR
 */
const val ASCII_GS: UByte = 29u

/**
 * ASCII Group Separator
 *
 * ```
 * '\u001D'
 * ```
 *
 * @see ASCII_GS
 *
 * @since 1.1.0
 */
const val ASCII_GROUP_SEPARATOR: UByte = 29u

/**
 * ASCII Record Separator
 *
 * ```
 * '\u001E'
 * ```
 *
 * @see ASCII_RECORD_SEPARATOR
 */
const val ASCII_RS: UByte = 30u

/**
 * ASCII Record Separator
 *
 * ```
 * '\u001E'
 * ```
 *
 * @see ASCII_RS
 *
 * @since 1.1.0
 */
const val ASCII_RECORD_SEPARATOR: UByte = 30u

/**
 * ASCII Unit Separator
 *
 * ```
 * '\u001F'
 * ```
 *
 * @see ASCII_UNIT_SEPARATOR
 */
const val ASCII_US: UByte = 31u

/**
 * ASCII Unit Separator
 *
 * ```
 * '\u001F'
 * ```
 *
 * @see ASCII_US
 *
 * @since 1.1.0
 */
const val ASCII_UNIT_SEPARATOR: UByte = 31u

/**
 * ASCII Space
 *
 * ```
 * ' '
 * ```
 */
const val ASCII_SPACE: UByte = 32u

/**
 * ASCII Exclamation Mark
 *
 * ```
 * '!'
 * ```
 */
const val ASCII_EXCLAIM: UByte = 33u

/**
 * ASCII Double Quote
 *
 * ```
 * '"'
 * ```
 */
const val ASCII_DOUBLE_QUOTE: UByte = 34u

/**
 * ASCII Number Sign
 *
 * ```
 * '#'
 * ```
 *
 * @see ASCII_HASH
 * @see ASCII_POUND
 */
const val ASCII_NUMBER: UByte = 35u

/**
 * ASCII Number Sign
 *
 * ```
 * '#'
 * ```
 *
 * @see ASCII_NUMBER
 * @see ASCII_POUND
 *
 * @since 1.1.0
 */
const val ASCII_HASH: UByte = 35u

/**
 * ASCII Number Sign
 *
 * ```
 * '#'
 * ```
 *
 * @see ASCII_NUMBER
 * @see ASCII_HASH
 *
 * @since 1.1.0
 */
const val ASCII_POUND: UByte = 35u

/**
 * ASCII Dollar Sign
 *
 * ```
 * '$'
 * ```
 */
const val ASCII_DOLLAR: UByte = 36u

/**
 * ASCII Percent Sign
 *
 * ```
 * '%'
 * ```
 */
const val ASCII_PERCENT: UByte = 37u

/**
 * ASCII Ampersand
 *
 * ```
 * '&'
 * ```
 *
 * @see ASCII_AMPERSAND
 */
const val ASCII_AMP: UByte = 38u

/**
 * ASCII Ampersand
 *
 * ```
 * '&'
 * ```
 *
 * @see ASCII_AMP
 *
 * @since 1.1.0
 */
const val ASCII_AMPERSAND: UByte = 38u

/**
 * ASCII Single Quote
 *
 * ```
 * '\''
 * ```
 *
 * @see ASCII_APOSTROPHE
 * @see ASCII_SINGLE_QUOTE
 */
const val ASCII_APOS: UByte = 39u

/**
 * ASCII Single Quote
 *
 * ```
 * '\''
 * ```
 *
 * @see ASCII_APOS
 * @see ASCII_SINGLE_QUOTE
 *
 * @since 1.1.0
 */
const val ASCII_APOSTROPHE: UByte = 39u

/**
 * ASCII Single Quote
 *
 * ```
 * '\''
 * ```
 *
 * @see ASCII_APOS
 * @see ASCII_APOSTROPHE
 *
 * @since 1.1.0
 */
const val ASCII_SINGLE_QUOTE: UByte = 39u

/**
 * ASCII Left Parenthesis
 *
 * ```
 * '('
 * ```
 */
const val ASCII_PAREN_OPEN: UByte = 40u

/**
 * ASCII Right Parenthesis
 *
 * ```
 * ')'
 * ```
 */
const val ASCII_PAREN_CLOSE: UByte = 41u

/**
 * ASCII Asterisk
 *
 * ```
 * '*'
 * ```
 */
const val ASCII_ASTERISK: UByte = 42u

/**
 * ASCII Plus Sign
 *
 * ```
 * '+'
 * ```
 */
const val ASCII_PLUS: UByte = 43u

/**
 * ASCII Comma
 *
 * ```
 * ','
 * ```
 */
const val ASCII_COMMA: UByte = 44u

/**
 * ASCII Minus Sign
 *
 * ```
 * '-'
 * ```
 *
 * @see ASCII_DASH
 * @see ASCII_HYPHEN
 */
const val ASCII_MINUS: UByte = 45u

/**
 * ASCII Minus Sign
 *
 * ```
 * '-'
 * ```
 *
 * @see ASCII_MINUS
 * @see ASCII_HYPHEN
 *
 * @since 1.1.0
 */
const val ASCII_DASH: UByte = 45u

/**
 * ASCII Minus Sign
 *
 * ```
 * '-'
 * ```
 *
 * @see ASCII_MINUS
 * @see ASCII_DASH
 *
 * @since 1.1.0
 */
const val ASCII_HYPHEN: UByte = 45u

/**
 * ASCII Period
 *
 * ```
 * '.'
 * ```
 */
const val ASCII_PERIOD: UByte = 46u

/**
 * ASCII Forward Slash
 *
 * ```
 * '/'
 * ```
 */
const val ASCII_SLASH: UByte = 47u

/**
 * ASCII Digit 0
 *
 * ```
 * '0'
 * ```
 */
const val ASCII_DIGIT_0: UByte = 48u

/**
 * ASCII Digit 1
 *
 * ```
 * '1'
 * ```
 */
const val ASCII_DIGIT_1: UByte = 49u

/**
 * ASCII Digit 2
 *
 * ```
 * '2'
 * ```
 */
const val ASCII_DIGIT_2: UByte = 50u

/**
 * ASCII Digit 3
 *
 * ```
 * '3'
 * ```
 */
const val ASCII_DIGIT_3: UByte = 51u

/**
 * ASCII Digit 4
 *
 * ```
 * '4'
 * ```
 */
const val ASCII_DIGIT_4: UByte = 52u

/**
 * ASCII Digit 5
 *
 * ```
 * '5'
 * ```
 */
const val ASCII_DIGIT_5: UByte = 53u

/**
 * ASCII Digit 6
 *
 * ```
 * '6'
 * ```
 */
const val ASCII_DIGIT_6: UByte = 54u

/**
 * ASCII Digit 7
 *
 * ```
 * '7'
 * ```
 */
const val ASCII_DIGIT_7: UByte = 55u

/**
 * ASCII Digit 8
 *
 * ```
 * '8'
 * ```
 */
const val ASCII_DIGIT_8: UByte = 56u

/**
 * ASCII Digit 9
 *
 * ```
 * '9'
 * ```
 */
const val ASCII_DIGIT_9: UByte = 57u

/**
 * ASCII Colon
 *
 * ```
 * ':'
 * ```
 */
const val ASCII_COLON: UByte = 58u

/**
 * ASCII Semicolon
 *
 * ```
 * ';'
 * ```
 */
const val ASCII_SEMICOLON: UByte = 59u

/**
 * ASCII Less Than Sign
 *
 * ```
 * '<'
 * ```
 *
 * @see ASCII_LESS_THAN
 */
const val ASCII_LESS: UByte = 60u

/**
 * ASCII Less Than Sign
 *
 * ```
 * '<'
 * ```
 *
 * @see ASCII_LESS
 *
 * @since 1.1.0
 */
const val ASCII_LESS_THAN: UByte = 60u

/**
 * ASCII Equals Sign
 *
 * ```
 * '='
 * ```
 */
const val ASCII_EQUALS: UByte = 61u

/**
 * ASCII Greater Than Sign
 *
 * ```
 * '>'
 * ```
 *
 * @see ASCII_GREATER_THAN
 *
 * @since 1.1.0
 */
const val ASCII_GREATER: UByte = 62u

/**
 * ASCII Greater Than Sign
 *
 * ```
 * '>'
 * ```
 *
 * @see ASCII_GREATER
 */
const val ASCII_GREATER_THAN: UByte = 62u

/**
 * ASCII Question Mark
 *
 * ```
 * '?'
 * ```
 */
const val ASCII_QUESTION: UByte = 63u

/**
 * ASCII At/Address Sign
 *
 * ```
 * '@'
 * ```
 */
const val ASCII_AT: UByte = 64u

/**
 * ASCII Uppercase Letter A
 *
 * ```
 * 'A'
 * ```
 */
const val ASCII_UPPER_A: UByte = 65u

/**
 * ASCII Uppercase Letter B
 *
 * ```
 * 'B'
 * ```
 */
const val ASCII_UPPER_B: UByte = 66u

/**
 * ASCII Uppercase Letter C
 *
 * ```
 * 'C'
 * ```
 */
const val ASCII_UPPER_C: UByte = 67u

/**
 * ASCII Uppercase Letter D
 *
 * ```
 * 'D'
 * ```
 */
const val ASCII_UPPER_D: UByte = 68u

/**
 * ASCII Uppercase Letter E
 *
 * ```
 * 'E'
 * ```
 */
const val ASCII_UPPER_E: UByte = 69u

/**
 * ASCII Uppercase Letter F
 *
 * ```
 * 'F'
 * ```
 */
const val ASCII_UPPER_F: UByte = 70u

/**
 * ASCII Uppercase Letter G
 *
 * ```
 * 'G'
 * ```
 */
const val ASCII_UPPER_G: UByte = 71u

/**
 * ASCII Uppercase Letter H
 *
 * ```
 * 'H'
 * ```
 */
const val ASCII_UPPER_H: UByte = 72u

/**
 * ASCII Uppercase Letter I
 *
 * ```
 * 'I'
 * ```
 */
const val ASCII_UPPER_I: UByte = 73u

/**
 * ASCII Uppercase Letter J
 *
 * ```
 * 'J'
 * ```
 */
const val ASCII_UPPER_J: UByte = 74u

/**
 * ASCII Uppercase Letter K
 *
 * ```
 * 'K'
 * ```
 */
const val ASCII_UPPER_K: UByte = 75u

/**
 * ASCII Uppercase Letter L
 *
 * ```
 * 'L'
 * ```
 */
const val ASCII_UPPER_L: UByte = 76u

/**
 * ASCII Uppercase Letter M
 *
 * ```
 * 'M'
 * ```
 */
const val ASCII_UPPER_M: UByte = 77u

/**
 * ASCII Uppercase Letter N
 *
 * ```
 * 'N'
 * ```
 */
const val ASCII_UPPER_N: UByte = 78u

/**
 * ASCII Uppercase Letter O
 *
 * ```
 * 'O'
 * ```
 */
const val ASCII_UPPER_O: UByte = 79u

/**
 * ASCII Uppercase Letter P
 *
 * ```
 * 'P'
 * ```
 */
const val ASCII_UPPER_P: UByte = 80u

/**
 * ASCII Uppercase Letter Q
 *
 * ```
 * 'Q'
 * ```
 */
const val ASCII_UPPER_Q: UByte = 81u

/**
 * ASCII Uppercase Letter R
 *
 * ```
 * 'R'
 * ```
 */
const val ASCII_UPPER_R: UByte = 82u

/**
 * ASCII Uppercase Letter S
 *
 * ```
 * 'S'
 * ```
 */
const val ASCII_UPPER_S: UByte = 83u

/**
 * ASCII Uppercase Letter T
 *
 * ```
 * 'T'
 * ```
 */
const val ASCII_UPPER_T: UByte = 84u

/**
 * ASCII Uppercase Letter U
 *
 * ```
 * 'U'
 * ```
 */
const val ASCII_UPPER_U: UByte = 85u

/**
 * ASCII Uppercase Letter V
 *
 * ```
 * 'V'
 * ```
 */
const val ASCII_UPPER_V: UByte = 86u

/**
 * ASCII Uppercase Letter W
 *
 * ```
 * 'W'
 * ```
 */
const val ASCII_UPPER_W: UByte = 87u

/**
 * ASCII Uppercase Letter X
 *
 * ```
 * 'X'
 * ```
 */
const val ASCII_UPPER_X: UByte = 88u

/**
 * ASCII Uppercase Letter Y
 *
 * ```
 * 'Y'
 * ```
 */
const val ASCII_UPPER_Y: UByte = 89u

/**
 * ASCII Uppercase Letter Z
 *
 * ```
 * 'Z'
 * ```
 */
const val ASCII_UPPER_Z: UByte = 90u

/**
 * ASCII Left Square Bracket
 *
 * ```
 * '['
 * ```
 */
const val ASCII_SQUARE_OPEN: UByte = 91u

/**
 * ASCII Backslash
 *
 * ```
 * '\\'
 * ```
 */
const val ASCII_BACKSLASH: UByte = 92u

/**
 * ASCII Right Square Bracket
 *
 * ```
 * ']'
 * ```
 */
const val ASCII_SQUARE_CLOSE: UByte = 93u

/**
 * ASCII Caret / Circumflex
 *
 * ```
 * '^'
 * ```
 */
const val ASCII_CARET: UByte = 94u

/**
 * ASCII Underscore
 *
 * ```
 * '_'
 * ```
 */
const val ASCII_UNDER: UByte = 95u

/**
 * ASCII Grave Accent
 *
 * ```
 * '`'
 * ```
 */
const val ASCII_GRAVE: UByte = 96u

/**
 * ASCII Lowercase Letter A
 *
 * ```
 * 'a'
 * ```
 */
const val ASCII_LOWER_A: UByte = 97u

/**
 * ASCII Lowercase Letter B
 *
 * ```
 * 'b'
 * ```
 */
const val ASCII_LOWER_B: UByte = 98u

/**
 * ASCII Lowercase Letter C
 *
 * ```
 * 'c'
 * ```
 */
const val ASCII_LOWER_C: UByte = 99u

/**
 * ASCII Lowercase Letter D
 *
 * ```
 * 'd'
 * ```
 */
const val ASCII_LOWER_D: UByte = 100u

/**
 * ASCII Lowercase Letter E
 *
 * ```
 * 'e'
 * ```
 */
const val ASCII_LOWER_E: UByte = 101u

/**
 * ASCII Lowercase Letter F
 *
 * ```
 * 'f'
 * ```
 */
const val ASCII_LOWER_F: UByte = 102u

/**
 * ASCII Lowercase Letter G
 *
 * ```
 * 'g'
 * ```
 */
const val ASCII_LOWER_G: UByte = 103u

/**
 * ASCII Lowercase Letter H
 *
 * ```
 * 'h'
 * ```
 */
const val ASCII_LOWER_H: UByte = 104u

/**
 * ASCII Lowercase Letter I
 *
 * ```
 * 'i'
 * ```
 */
const val ASCII_LOWER_I: UByte = 105u

/**
 * ASCII Lowercase Letter J
 *
 * ```
 * 'j'
 * ```
 */
const val ASCII_LOWER_J: UByte = 106u

/**
 * ASCII Lowercase Letter K
 *
 * ```
 * 'k'
 * ```
 */
const val ASCII_LOWER_K: UByte = 107u

/**
 * ASCII Lowercase Letter L
 *
 * ```
 * 'l'
 * ```
 */
const val ASCII_LOWER_L: UByte = 108u

/**
 * ASCII Lowercase Letter M
 *
 * ```
 * 'm'
 * ```
 */
const val ASCII_LOWER_M: UByte = 109u

/**
 * ASCII Lowercase Letter N
 *
 * ```
 * 'n'
 * ```
 */
const val ASCII_LOWER_N: UByte = 110u

/**
 * ASCII Lowercase Letter O
 *
 * ```
 * 'o'
 * ```
 */
const val ASCII_LOWER_O: UByte = 111u

/**
 * ASCII Lowercase Letter P
 *
 * ```
 * 'p'
 * ```
 */
const val ASCII_LOWER_P: UByte = 112u

/**
 * ASCII Lowercase Letter Q
 *
 * ```
 * 'q'
 * ```
 */
const val ASCII_LOWER_Q: UByte = 113u

/**
 * ASCII Lowercase Letter R
 *
 * ```
 * 'r'
 * ```
 */
const val ASCII_LOWER_R: UByte = 114u

/**
 * ASCII Lowercase Letter S
 *
 * ```
 * 's'
 * ```
 */
const val ASCII_LOWER_S: UByte = 115u

/**
 * ASCII Lowercase Letter T
 *
 * ```
 * 't'
 * ```
 */
const val ASCII_LOWER_T: UByte = 116u

/**
 * ASCII Lowercase Letter U
 *
 * ```
 * 'u'
 * ```
 */
const val ASCII_LOWER_U: UByte = 117u

/**
 * ASCII Lowercase Letter V
 *
 * ```
 * 'v'
 * ```
 */
const val ASCII_LOWER_V: UByte = 118u

/**
 * ASCII Lowercase Letter W
 *
 * ```
 * 'w'
 * ```
 */
const val ASCII_LOWER_W: UByte = 119u

/**
 * ASCII Lowercase Letter X
 *
 * ```
 * 'x'
 * ```
 */
const val ASCII_LOWER_X: UByte = 120u

/**
 * ASCII Lowercase Letter Y
 *
 * ```
 * 'y'
 * ```
 */
const val ASCII_LOWER_Y: UByte = 121u

/**
 * ASCII Lowercase Letter Z
 *
 * ```
 * 'z'
 * ```
 */
const val ASCII_LOWER_Z: UByte = 122u

/**
 * ASCII Left Curly Bracket
 *
 * ```
 * '{'
 * ```
 */
const val ASCII_CURLY_OPEN: UByte = 123u

/**
 * ASCII Vertical Line
 *
 * ```
 * '|'
 * ```
 *
 * @see ASCII_VERTICAL_LINE
 */
const val ASCII_PIPE: UByte = 124u

/**
 * ASCII Vertical Line
 *
 * ```
 * '|'
 * ```
 *
 * @see ASCII_PIPE
 *
 * @since 1.1.0
 */
const val ASCII_VERTICAL_LINE: UByte = 124u

/**
 * ASCII Right Curly Bracket
 *
 * ```
 * '}'
 * ```
 */
const val ASCII_CURLY_CLOSE: UByte = 125u

/**
 * ASCII Tilde
 *
 * ```
 * '~'
 * ```
 */
const val ASCII_TILDE: UByte = 126u

/**
 * ASCII Delete
 *
 * ```
 * '\u007F'
 * ```
 */
const val ASCII_DELETE: UByte = 127u
